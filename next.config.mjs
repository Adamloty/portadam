/** @type {import('next').NextConfig} */
const nextConfig = {  images: {
    domains: ['exwpcoder.com', 'another-domain.com'], // Add your allowed domains here
  }};

  
  
export default nextConfig;
